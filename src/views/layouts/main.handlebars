<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>main</title>
    <style>
        .conteudo{
            border: 1px solid black;
        }
        
        body {
            background-color: #747474;
            font-family: "Arial", sans-serif;
            overflow-x: hidden;
        }

        .post-label {
            font-size: 24px;
            margin: 20px 0;
            padding-bottom: 10px;
            border-bottom: 1px solid #ccc;
        }

        .post {
            margin-bottom: 20px;
            padding: 10px;
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .post-title {
            color: black;
            font-size: 20px;
            margin: 0;
        }

        .post-author {
            font-size: 14px;
            color: #888;
            margin-top: 5px;
        }

        .post-author a {
            color: #333;
            text-decoration: none;
        }

        .post-content {
            color: black;
            margin-top: 10px;
            font-size: 16px;
        }

        a{
            color: black;
            text-decoration: none;
        }

        div > a{
            color: rgb(255, 255, 255);
            text-decoration: none;
        }

        .header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: black;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            color: white;
        }

        .header-text {
            width: 80%;
            font-size: 24px;
        }

        .subheader-text {
            width: 20%;
            font-size: 16px;
            text-align: center;
        }

        .subheader-text a {
            color: white;
            text-decoration: none;
        }

        .content {
            margin-top: 100px; /* Adicione margem superior para compensar o cabeçalho fixo */
            padding: 20px;
            color: #fff;
            font-family: "Arial", sans-serif;
        }

        .box {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"],
        textarea {
            padding: 8px;
            font-size: 16px;
        }

        button[type="submit"] {
            margin-top: 10px;
            padding: 10px 20px;
            font-size: 18px;
            background-color: #ffcc00;
            color: #333;
            border: none;
            cursor: pointer;
        }

        .box1{
            display: flex;
            flex-direction: column;
            border: 1px solid black;
            padding: 10px;
            background-color: rgb(49, 49, 49);
        }

        .login-container {
            max-width: 400px;
            margin: 0 auto;
            padding: 40px;
            background-color: #fff;
            border-radius: 4px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .login-heading {
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
        }

        .login-form-group {
            margin-bottom: 20px;
        }

        .login-label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: black !important;
        }

        .login-input {
            width: 100%;
            padding: 8px;
            font-size: 16px;
        }

        .login-button {
            padding: 10px 20px;
            font-size: 18px;
            background-color: #ffcc00;
            color: #333;
            border: none;
            cursor: pointer;
        }

        .registro-container {
            max-width: 400px;
            margin: 0 auto;
            padding: 40px;
            background-color: #fff;
            border-radius: 4px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .registro-heading {
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
        }

        .registro-form-group {
            margin-bottom: 20px;
        }

        .registro-label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        .registro-input {
            width: 100%;
            padding: 8px;
            font-size: 16px;
        }

        .registro-button {
            padding: 10px 20px;
            font-size: 18px;
            background-color: #ffcc00;
            color: #333;
            border: none;
            cursor: pointer;
        }

        .profile-container {
            max-width: 400px;
            margin: 0 auto;
            padding: 40px;
            background-color: #fff;
            border-radius: 4px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .profile-heading {
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
        }

        .profile-info {
            margin-bottom: 20px;
        }

        .profile-label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        .profile-value {
            font-size: 16px;
        }

    /* Estilo das categorias... */
    .categorias {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        max-width: 80%;
        margin: 0 auto;
        padding: 20px;
    }

    .category {
        /* Estilo da categoria... */
        margin: 10px;
        padding: 10px;
        background-color: #f0f0f0;
        border: 1px solid #ccc;
        border-radius: 4px;
        width: 250px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .category-title {
        /* Estilo do título da categoria... */
        font-size: 20px;
        margin: 0;
        padding-bottom: 5px;
        border-bottom: 1px solid #ccc;
        color: #333;
    }

    .last-post-title {
        /* Estilo do título do último post... */
        font-size: 16px;
        margin: 5px 0;
        color: #666;
    }

    .last-post-author {
        /* Estilo do autor do último post... */
        font-size: 14px;
        color: #888;
        margin: 0;
    }

    .no-post-message {
        /* Estilo da mensagem para categoria sem post... */
        font-size: 14px;
        color: #888;
        margin: 0;
    }
    </style>
</head>
<body>
    {{{body}}}
</body>
</html>